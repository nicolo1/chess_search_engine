const MatchHistory = ({ matchHistory }) => {
    if(matchHistory) {
        return (  
            <div id='match-history-component-container'> 
                Match History
            </div>
        );
    }
}
 
export default MatchHistory;